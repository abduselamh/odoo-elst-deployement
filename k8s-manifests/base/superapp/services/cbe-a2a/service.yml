apiVersion: v1
kind: Service
metadata:
  name: member-auth
  labels:
    app: member-auth
spec:
  selector:
    app: member-auth
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP