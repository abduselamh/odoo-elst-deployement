apiVersion: v1
kind: Service
metadata:
  name: cbe-to-cbe
  labels:
    app: cbe-to-cbe
spec:
  selector:
    app: cbe-to-cbe
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP