apiVersion: v1
kind: Service
metadata:
  name: cps-action
  labels:
    app: cps-action
spec:
  selector:
    app: cps-action
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP