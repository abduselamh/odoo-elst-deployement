apiVersion: v1
kind: Service
metadata:
  name: cps-auth
  labels:
    app: cps-auth
spec:
  selector:
    app: cps-auth
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP