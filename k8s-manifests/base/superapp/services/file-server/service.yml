apiVersion: v1
kind: Service
metadata:
  name: file-server
  labels:
    app: file-server
spec:
  selector:
    app: file-server
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP