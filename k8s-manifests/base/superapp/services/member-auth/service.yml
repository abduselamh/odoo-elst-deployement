apiVersion: v1
kind: Service
metadata:
  name: member-auth
  labels:
    app: member-auth
spec:
  selector:
    app: member-auth
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
    - name: grpc
      port: 50051
      targetPort: 50051
      protocol: TCP
  type: ClusterIP