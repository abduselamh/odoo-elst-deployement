apiVersion: apps/v1
kind: Deployment
metadata:
  name: migration
  labels:
    app:  migration
    project: cbe-superapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app:  migration
  template:
    metadata:
      labels:
        app:  migration
        project: cbe-superapp
    spec:
      imagePullSecrets:
        - name: regcred 
      containers:
      - name:  migration
        image: harbor.cbe.com.et/cbe-superapp/migration-dev:latest
        imagePullPolicy: Always
        ports:
          - containerPort: 8080
        envFrom:
          - configMapRef:
              name: superapp-env-configmap