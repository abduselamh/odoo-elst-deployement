apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification
  labels:
    app:  notification
    project: cbe-superapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app:  notification
  template:
    metadata:
      labels:
        app:  notification
        project: cbe-superapp
    spec:
      imagePullSecrets:
        - name: regcred 
      containers:
      - name:  notification
        image: harbor.cbe.com.et/cbe-superapp/notification-dev:latest
        imagePullPolicy: Always
        ports:
          - containerPort: 8080
        envFrom:
          - configMapRef:
              name: superapp-env-configmap