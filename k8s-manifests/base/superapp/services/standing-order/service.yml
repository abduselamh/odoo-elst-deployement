apiVersion: v1
kind: Service
metadata:
  name: standing-order
  labels:
    app: standing-order
spec:
  selector:
    app: standing-order
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP