apiVersion: v1
kind: Service
metadata:
  name: three-click-auth
  labels:
    app: three-click-auth
spec:
  selector:
    app: three-click-auth
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP