apiVersion: v1
kind: Service
metadata:
  name: three-click-delivery
  labels:
    app: three-click-delivery
spec:
  selector:
    app: three-click-delivery
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP