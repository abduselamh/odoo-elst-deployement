apiVersion: apps/v1
kind: Deployment
metadata:
  name: three-click-order
  labels:
    app:  three-click-order
    project: cbe-superapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app:  three-click-order
  template:
    metadata:
      labels:
        app:  three-click-order
        project: cbe-superapp
    spec:
      imagePullSecrets:
        - name: regcred 
      containers:
      - name:  three-click-order
        image: superapp-registry.cbe.com.et:5000/cbe-superapp/three-click-order-dev:latest
        imagePullPolicy: Always
        ports:
          - containerPort: 8080
        envFrom:
          - configMapRef:
              name: superapp-env-configmap