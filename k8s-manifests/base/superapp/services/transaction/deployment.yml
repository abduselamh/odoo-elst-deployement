apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction
  labels:
    app:  transaction
    project: cbe-superapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app:  transaction
  template:
    metadata:
      labels:
        app:  transaction
        project: cbe-superapp
    spec:
      imagePullSecrets:
        - name: regcred 
      containers:
      - name:  transaction
        image: harbor.cbe.com.et/cbe-superapp/transaction-dev:latest
        imagePullPolicy: Always
        ports:
          - containerPort: 8080
        envFrom:
          - configMapRef:
              name: superapp-env-configmap