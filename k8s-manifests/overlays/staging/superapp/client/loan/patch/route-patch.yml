apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: yabex-route
spec:
  path: /api/v1/cbesuperapp/yabex
  to:
    name: yabex-qa
    weight: 100
    kind: Service
  host: qaapisuperapp.cbe.com.et
  port:
    targetPort: 8080
