apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: split-payment-route

spec:
  path: /api/v1/cbesuperapp/split_payment
  to:
    name: split-payment-qa
    weight: 100
    kind: Service
  host: qaapisuperapp.cbe.com.et
  port:
    targetPort: 8080

---

apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: mini-app-route

spec:
  path: /api/v1/cbesuperapp/mini-apps
  to:
    name: mini-app-qa
    weight: 100
    kind: Service
  host: qaapisuperapp.cbe.com.et
  port:
    targetPort: 8080





